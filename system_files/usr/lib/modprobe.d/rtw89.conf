# Disable rtw89 firmware low-power mode to stop periodic latency spikes to gateway.
# iw/NetworkManager power_save does NOT control this; only this module param does.
options rtw89_core disable_ps_mode=Y
